[daemon]
# Warbler opt-in kiosk autologin. The login user is created at install time;
# never bake in a known password (see AGENTS.md safety).
# AutomaticLoginEnable=true
# AutomaticLogin=
WaylandEnable=true
