# Upstream Mango default config (phase-5).
# User overrides in ~/.config/mango/config.conf
# Source: DreamMaoMao/mango-config (daily default) or as-shipped /etc/mango/config.conf

[core]
# output = auto
# scale = auto
# refresh_rate = max

[input]
# keyboard_layout = us
# keyboard_model = pc105
# keyboard_options = caps:escape

[appearance]
# theme = dark
# wallpaper =

[autostart]
# Commands to run on startup
# quickshell = quickshell
# awww = awww