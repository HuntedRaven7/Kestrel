#!/usr/bin/env bash
# install-packages.py equivalent entry (phase-4 stub). Reads warbler.toml contract.
set -euo pipefail
echo "TODO: resolve + install warbler.toml from base + /etc/rpm-factory repo"
